package com.example.techchallenge.data.db

class DbDataMapper {

//    fun convertFromDomain(forecast: ForecastList) = with(forecast) {
//        val daily = dailyForecast.map { convertDayFromDomain(id, it) }
//        CityForecast(id, city, country, daily)
//    }
//
//    private fun convertDayFromDomain(cityId: Long, forecast: Forecast) = with(forecast) {
//        DayForecast(date, description, high, low, iconUrl, cityId)
//    }

//    fun convertToDataClasses(visits: Visit) = with(visits) {
//        val stats = stats.map { convertStatToDomain(it) }
//        ForecastList(_id, startTime, endTime, stats)
//    }
//
//    fun convertStatToDomain(stat: Stat) = with(stat) {
//        Stat(_id, visitId, time, location, category)
//    }
}